<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medklik.medklik">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BuktiTransaksiActivity"></activity>
        <activity android:name=".PinActivity" />
        <activity android:name=".AmbilKodeBookingActivity" />
        <activity android:name=".PembayaranDataPasienActivity" />
        <activity android:name=".ui.login.LoginPenggunaActivity" />
        <activity android:name=".ui.login.KodeVerifikasiActivity" />
        <activity android:name=".ui.login.MethodVerifikasiActivity" /> <!-- <activity -->
        <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login" /> -->
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
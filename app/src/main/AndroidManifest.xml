<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medklik.medklik_user">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailApotekActivity"></activity>
        <activity android:name=".BelanjaObatActivity" />
        <activity
            android:name=".DetailKlinikActivity"
            android:label="@string/title_activity_detail_klinik"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ButuhMedisActivity" />
        <activity android:name=".LihatPetaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>